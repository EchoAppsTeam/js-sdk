Ext.data.JsonP.dependency_management({
  "guide": "<h1 id='dependency_management-section-dependency-management-in-echo-js-sdk'>Dependency management in Echo JS SDK</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dependency_management-section-overview'>Overview</a></li>\n<li><a href='#!/guide/dependency_management-section-specifying-dependencies-for-an-app'>Specifying dependencies for an App</a></li>\n<li><a href='#!/guide/dependency_management-section-specifying-dependencies-for-a-plugin'>Specifying dependencies for a Plugin</a></li>\n<li><a href='#!/guide/dependency_management-section-specifying-dependencies-to-load-on-demand'>Specifying dependencies to load on demand</a></li>\n</ol>\n</div>\n\n<p>Modern browsers optimize their work with web resources in the many different ways, working with HTTP connections is not the exception here. Browser is able to handle several connections simultaneously. This fact allows to decrease application initialization time significantly as a result decreasing page rendering time.</p>\n\n<h2 id='dependency_management-section-overview'>Overview</h2>\n\n<p>As a result developer can design and build their application architecture better by dividing it into several modules without caring much that a big number of files will affect application performance or responsiveness. It was <a href=\"http://blog.getify.com/labjs-why-not-just-concat/\">shown</a> that parallel downloading of small files leads to better performance comparing with downloading of a single big concatenated file.</p>\n\n<p>Usually Javascript files downloaded via &lt;script> tag block page rendering and parsing of other code. This mode is named synchronous loading. Asynchronous loading doesn’t block page activity which is obviously much better. Figures 1 and 2 below illustrate this difference between modes.</p>\n\n<p><em>Figure 1. Synchronous scripts loading</em></p>\n\n<p><img src=\"guides/dependency_management/figure1.png\" alt=\"Figure 1. Synchronous scripts loading\" width=\"100%\"><br><br></p>\n\n<p><em>Figure 2. Asynchronous scripts loading using <a href=\"#!/api/Echo.Loader-static-method-download\" rel=\"Echo.Loader-static-method-download\" class=\"docClass\">Echo.Loader.download</a> method</em></p>\n\n<p><img src=\"guides/dependency_management/figure2.png\" alt=\"Figure 2. Asynchronous scripts loading using Echo.Loader.download method\" width=\"100%\"><br><br></p>\n\n<p>Both figures show that scripts are downloaded in parallel. Synchronous mode makes the DOM available in 771ms (figure 1) while asynchronous is about 3 times faster - DOM is available in 232ms (figure 2).</p>\n\n<p><a href=\"#!/api/Echo.Loader-static-method-download\" rel=\"Echo.Loader-static-method-download\" class=\"docClass\">Echo.Loader.download</a> method accepts a list of resources to load as a first argument. Resources here are not only Javascript files but CSS files also. This method loads files in parallel in the arbitrary order but executes them in the order they were specified. This logic allows to download resources as fast as possible preserving the dependencies execution order at the same time.</p>\n\n<p>Properly designed application should be divided into modules. In the Echo JS SDK each module (or a number of dependent modules) is represented as a separate .js file. There is no need to put everything in one file because these separate modules can be loaded on a separate basis when they are needed.</p>\n\n<p>Application might need some module functionality during its initialization or when some action happens, for example user clicks on some link.</p>\n\n<h2 id='dependency_management-section-specifying-dependencies-for-an-app'>Specifying dependencies for an App</h2>\n\n<p>While developing an application (see the <a href=\"#!/guide/how_to_develop_app\">“how to guide”</a>) you may specify some modules the application is dependent on during its initialization. Here is the example how to do it:</p>\n\n<pre><code>Comments.dependencies = [{\n    \"loaded\": function() {\n        return <a href=\"#!/api/Echo.App-static-method-isDefined\" rel=\"Echo.App-static-method-isDefined\" class=\"docClass\">Echo.App.isDefined</a>(\"<a href=\"#!/api/Echo.StreamServer.Controls.Submit\" rel=\"Echo.StreamServer.Controls.Submit\" class=\"docClass\">Echo.StreamServer.Controls.Submit</a>\") &amp;&amp;\n            <a href=\"#!/api/Echo.App-static-method-isDefined\" rel=\"Echo.App-static-method-isDefined\" class=\"docClass\">Echo.App.isDefined</a>(\"<a href=\"#!/api/Echo.StreamServer.Controls.Stream\" rel=\"Echo.StreamServer.Controls.Stream\" class=\"docClass\">Echo.StreamServer.Controls.Stream</a>\");\n    },\n    \"url\": \"{config:cdnBaseURL.sdk}/streamserver.pack.js\"\n}];\n</code></pre>\n\n<p>This code describes a single dependency which will be loaded only if there are no Stream and Submit Apps on the page already. More details on what happens in this example can be found <a href=\"#!/guide/how_to_develop_app-section-8\">here</a>.</p>\n\n<h2 id='dependency_management-section-specifying-dependencies-for-a-plugin'>Specifying dependencies for a Plugin</h2>\n\n<p>Your plugin might also rely on some external resources required for its initialization. Echo JS SDK engine provides an ability to define these dependencies within the Plugin manifest. More information about the plugin dependencies definition can be found in the <a href=\"#!/guide/how_to_develop_plugin-section-12\">\"How to develop a Plugin\" guide</a>.</p>\n\n<h2 id='dependency_management-section-specifying-dependencies-to-load-on-demand'>Specifying dependencies to load on demand</h2>\n\n<p>Some dependencies are needed only when some activity happens on the page like user clicking some button or reacting to server response. In this case there is no need of loading such resources during application initialization. More correct way to do it is to use <a href=\"#!/api/Echo.Loader-static-method-download\" rel=\"Echo.Loader-static-method-download\" class=\"docClass\">Echo.Loader.download</a> method mentioned above. Let’s look how to work with on-demand dependencies.</p>\n\n<p>Imagine we develop the application dealing with user social activity. User should be able not only to post some comments but also to share his activity in some social networks. One of the services providing this type of functionality is <a href=\"http://janrain.com/\">Janrain</a>. We don’t really want to load Janrain scripts every time we initialize our application because user might not share anything at all. Let’s look on some code downloading Janrain script on demand:</p>\n\n<pre><code>SomeApp.renderers.shareButton = function(element) {\n    element.click(function() {\n        <a href=\"#!/api/Echo.Loader-static-method-download\" rel=\"Echo.Loader-static-method-download\" class=\"docClass\">Echo.Loader.download</a>([{\n            “url”: “url_to_share_janrain_script”,\n            “loaded”: function() {\n                return !!SomeJanrainModule;\n            }\n        }], function() {\n            // some sharing logic here\n        });\n    });\n};\n</code></pre>\n\n<p>In this example firstly we download Janrain module and then do our own business logic. This trick allows us to shorten application initialization time. Note that <a href=\"#!/api/Echo.Loader-static-method-download\" rel=\"Echo.Loader-static-method-download\" class=\"docClass\">Echo.Loader.download</a> method always remembers scripts it loaded before. So it won’t load the same script many times on repetitious calls but instead will execute specified callback immediately.</p>\n",
  "title": "Dependency management"
});