Ext.data.JsonP.how_to_extend_templates({
  "guide": "<h1>How to extend templates</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/how_to_extend_templates-section-1'>Element removal</a></li>\n<li><a href='#!/guide/how_to_extend_templates-section-2'>Insertion of a new element</a></li>\n<li><a href='#!/guide/how_to_extend_templates-section-3'>Element replacement</a></li>\n<li><a href='#!/guide/how_to_extend_templates-section-4'>Plugin template modification</a></li>\n</ol>\n</div>\n\n<p>When developing the plugins, which extend the functionality of the existing controls and applications it is often required to change the HTML templates. The plugins mechanism allows to modify the app or control templates using the <a href=\"#!/api/Echo.Plugin-method-extendTemplate\" rel=\"Echo.Plugin-method-extendTemplate\" class=\"docClass\">Echo.Plugin.extendTemplate</a> method. Due to the fact that the control view is being assembled during its initialization, the <a href=\"#!/api/Echo.Plugin-method-extendTemplate\" rel=\"Echo.Plugin-method-extendTemplate\" class=\"docClass\">Echo.Plugin.extendTemplate</a> method should be called within the plugin.init as shown below:</p>\n\n<pre><code>plugin.init = function() {\n    this.extendTemplate(action, anchor, html);\n};\n</code></pre>\n\n<p>More info about the plugin development can be found in the <a href=\"#!/guide/how_to_develop_plugin\">\"How to develop a Plugin\"</a> guide.</p>\n\n<p>The <a href=\"#!/api/Echo.Plugin-method-extendTemplate\" rel=\"Echo.Plugin-method-extendTemplate\" class=\"docClass\">Echo.Plugin.extendTemplate</a> method has the following arguments:</p>\n\n<ul>\n<li>action - transformation action, the following options are available:\n\n<ul>\n<li>remove - the element removal</li>\n<li>replace - the replacement of an element by another one</li>\n<li>insertBefore - insertion of an element before the given element</li>\n<li>insertAfter - insertion of an element after the given element</li>\n<li>insertAsFirstChild - insertion of an element as the first child of the given element</li>\n<li>insertAsLastChild - insertion of an element as the last child of the given element</li>\n</ul>\n</li>\n<li>anchor - the name of the element which is transformed</li>\n<li>html - HTML templates to be used in the transformation, it can also be the function returning the HTML template</li>\n</ul>\n\n\n<p>Let's consider the most common use cases of the templates transformation.</p>\n\n<h2 id='how_to_extend_templates-section-1'>Element removal</h2>\n\n<p>The code below illustrates the initialization of the plugin which removes an item avatar in the Stream control Item:</p>\n\n<pre><code>plugin.init = function() {\n    this.extendTemplate(\"remove\", \"avatar\");\n};\n</code></pre>\n\n<p>The left side of the image below illustrates the Stream control, while the right side shows the Stream control where the avatars are disabled using the above described plugin:</p>\n\n<p><img src=\"guides/how_to_extend_templates/images/removal_before.png\" alt=\"Stream control\" />&nbsp;&nbsp;&nbsp;<img src=\"guides/how_to_extend_templates/images/removal_after.png\" alt=\"Stream control without avatars\" /></p>\n\n<p><em>Note</em>:  in case it is required to hide an item instead of its complete removal, it is better to accomplish this using CSS.\nThe following code can serve as an example:</p>\n\n<pre><code>plugin.css = \".{class:avatar} { display: none; }\";\n</code></pre>\n\n<h2 id='how_to_extend_templates-section-2'>Insertion of a new element</h2>\n\n<p>The <a href=\"#!/api/Echo.Plugin-method-extendTemplate\" rel=\"Echo.Plugin-method-extendTemplate\" class=\"docClass\">Echo.Plugin.extendTemplate</a> method provides several options of the new item insertion:</p>\n\n<ul>\n<li>insertBefore - insertion of an element before the given element</li>\n<li>insertAfter - insertion of an element after the given element</li>\n<li>insertAsFirstChild - insertion of an element as the first child of the given element</li>\n<li>insertAsLastChild - insertion of an element as the last child of the given element</li>\n</ul>\n\n\n<p>As an example illustrating the item insertion, we can use the plugin that displays the number of item likes underneath the item actor avatar:</p>\n\n<pre><code>plugin.init = function() {\n    this.extendTemplate(\"insertAfter\", \"avatar\", plugin.template);\n};\n\nplugin.template =\n    '&lt;span class=\"{plugin.class:likesCount}\"&gt;&lt;/span&gt;' +\n    '&lt;i class=\"icon-thumbs-up\"&gt;&lt;/i&gt;';\n\nplugin.renderers.likesCount = function(element) {\n    return element\n        .empty()\n        .append(this.component.get(\"data.object.accumulators.likesCount\"));\n};\n</code></pre>\n\n<p>The left side of the image below illustrates the Stream control, while the right side shows the Stream control that displays the number of item likes underneath the item actor avatar:</p>\n\n<p><img src=\"guides/how_to_extend_templates/images/insertion_before.png\" alt=\"Stream control\" />&nbsp;&nbsp;&nbsp;<img src=\"guides/how_to_extend_templates/images/insertion_after.png\" alt=\"Stream control with item likes count\" /></p>\n\n<h2 id='how_to_extend_templates-section-3'>Element replacement</h2>\n\n<p>Let's consider the case when it is required to replace the existing element by the one defined within the plugin.  For example we need to change the default display of the comment author name section in the HTML template containing the author name and the date the comment was posted:</p>\n\n<pre><code>plugin.labels = {\n    \"createdBy\": \"Created by: \",\n    \"on\": \"on\"\n};\n\nplugin.init = function() {\n    this.extendTemplate(\"replace\", \"authorName\", plugin.template);\n};\n\nplugin.template =\n    '&lt;div class=\"{plugin.class:header} echo-primaryFont echo-primaryColor\"&gt;' +\n        '{plugin.label:createdBy} &lt;span class=\"{plugin.class:author} echo-linkColor\"&gt;&lt;/span&gt;' +\n        '{plugin.label:on} &lt;span class=\"{plugin.class:publishedDate}\"&gt;&lt;/span&gt;' +\n    '&lt;/div&gt;';\n\nplugin.renderers.author = function(element) {\n    return element\n        .empty()\n        .append(this.component.get(\"data.actor.title\"));\n};\n\nplugin.renderers.publishedDate = function(element) {\n    var published = this.component.get(\"data.object.published\");\n    element.empty();\n    if (!published) return element;\n    var date = new Date(<a href=\"#!/api/Echo.Utils-static-method-timestampFromW3CDTF\" rel=\"Echo.Utils-static-method-timestampFromW3CDTF\" class=\"docClass\">Echo.Utils.timestampFromW3CDTF</a>(published) * 1000);\n    return element\n        .append(date.toLocaleDateString() + ', ' + date.toLocaleTimeString());\n};\n</code></pre>\n\n<p>The left side of the image below illustrates the Stream control, while the right side shows the Stream control where the display of the comment author name section is customised:</p>\n\n<p><img src=\"guides/how_to_extend_templates/images/replacement_before.png\" alt=\"Stream control\" />&nbsp;&nbsp;&nbsp;<img src=\"guides/how_to_extend_templates/images/replacement_after.png\" alt=\"Stream control with customized author name section\" /></p>\n\n<h2 id='how_to_extend_templates-section-4'>Plugin template modification</h2>\n\n<p>For example there is a Stream control with the \"Like\" plugin:</p>\n\n<p><img src=\"guides/how_to_extend_templates/images/plugins_before.png\" alt=\"Stream control with &quot;Like&quot; plugin\" /></p>\n\n<p>It is required to add a caption above the list of users who liked this comment. This can be achieved by creating a new plugin which will insert an HTML element containing the desired inscription directly above the element set by the \"Like\" plugin. The \"LikesCaption\" plugin code is shown below:</p>\n\n<pre><code>plugin.labels = {\n    \"header\": \"Liked by: \"\n};\n\nplugin.init = function() {\n    this.extendTemplate(\"insertBefore\", \"plugin-Like-likedBy\", plugin.template);\n};\n\nplugin.template =\n    '&lt;div class=\"{plugin.class:likesHeader}\"&gt;{plugin.label:header}&lt;/div&gt;';\n\n// css customization\nplugin.css =\n    '.echo-streamserver-controls-facepile-suffixText { display: none; }' +\n    '.{plugin.class:likesHeader} { margin-top: 10px; }';\n</code></pre>\n\n<p>As we can see in the code in order to access the \"likedBy\" element which was added by the \"Like\" plugin we use \"plugin-Like-likedBy\" construction which has the following format \"<em>plugin</em>-<em>the_plugin_name</em>-<em>the_name_of_the_element</em>\".\nPlease note that in order to access the elements of other plugins, your plugin should be placed in the \"plugins\" array after the plugins which contain the elements you want to access. Here is the example of the Stream control initialization for the provided use-case:</p>\n\n<pre><code><a href=\"#!/api/Echo.Loader-static-method-initApplication\" rel=\"Echo.Loader-static-method-initApplication\" class=\"docClass\">Echo.Loader.initApplication</a>({\n    \"script\": \"http://cdn.echoenabled.com/sdk/v3/streamserver.pack.js\",\n    \"component\": \"<a href=\"#!/api/Echo.StreamServer.Controls.Stream\" rel=\"Echo.StreamServer.Controls.Stream\" class=\"docClass\">Echo.StreamServer.Controls.Stream</a>\",\n    \"config\": {\n         // some config parameters...\n         \"plugins\": [{\n              \"name\": \"Like\"\n         }, {\n              \"name\": \"LikesCaption\"  // goes after \"Like\"\n         }]\n    }\n});\n</code></pre>\n\n<p>Here is the final view of the Stream Item including the \"Like\" and \"LikesCaption\" plugins:</p>\n\n<p><img src=\"guides/how_to_extend_templates/images/plugins_after.png\" alt=\"Stream control with &quot;Like&quot; and &quot;LikesCaption&quot; plugins\" /></p>\n\n<p>The <a href=\"#!/api/Echo.Plugin-method-extendTemplate\" rel=\"Echo.Plugin-method-extendTemplate\" class=\"docClass\">Echo.Plugin.extendTemplate</a> method is widely used across Echo standard plugins. In this guide we described the most common use-cases and Echo plugins code might be a good source of information for your specific use-cases. Feel free to use any plugin as a pattern for your own plugin.</p>\n",
  "title": "How to extend templates"
});